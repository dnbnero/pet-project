apiVersion: v1
kind: Service
metadata:
  name: ch
spec:
  clusterIP: None
  selector:
    app: clickhouse
  ports:
    - protocol: TCP
      port: 8123
--- 
apiVersion: v1
kind: Service
metadata:
  name: ch-nodeport
spec:
  type: NodePort
  selector:
    app: clickhouse
  ports:
    - port: 8123
---
apiVersion: v1
kind: Service
metadata:
  name: ch-prom
spec:
  clusterIP: None
  selector:
    app: clickhouse
  ports:
    - protocol: TCP
      port: 9363